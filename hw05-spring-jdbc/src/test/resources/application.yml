spring:
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true
      enabled: true
  shell:
    interactive:
      enabled: false
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql